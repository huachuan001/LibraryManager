# ===========================================
# Library Manager 应用程序配置文件
# ===========================================

# 服务器配置
# 设置应用程序运行的端口号
# 默认端口是8080，这里设置为8081避免冲突
server.port=8081

# Spring Boot应用配置
# 设置应用程序的名称，用于日志记录和监控
spring.application.name=library-manager

# ===========================================
# 数据库配置（未来扩展用）
# ===========================================
# spring.datasource.url=jdbc:mysql://localhost:3306/library_manager
# spring.datasource.username=root
# spring.datasource.password=password
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===========================================
# JPA/Hibernate配置（未来扩展用）
# ===========================================
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ===========================================
# 日志配置（未来扩展用）
# ===========================================
# logging.level.com.example.librarymanager=DEBUG
# logging.level.org.springframework.web=DEBUG
